apiVersion: apps/v1
kind: Deployment
metadata:
  name: voice-bank-bot
spec:
  replicas: 1
  selector:
    matchLabels:
      app: voice-bank-bot
  template:
    metadata:
      labels:
        app: voice-bank-bot
    spec:
      containers:
        - name: voice-bank-bot
          image: voice-bank-bot:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8020
          envFrom:
            - secretRef:
                name: voice-bank-bot-secrets
          resources:
            requests:
              cpu: 200m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          readinessProbe:
            httpGet: { path: /health, port: 8020 }
            initialDelaySeconds: 2
          livenessProbe:
            httpGet: { path: /health, port: 8020 }
            initialDelaySeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: voice-bank-bot
spec:
  selector:
    app: voice-bank-bot
  ports:
    - port: 80
      targetPort: 8020
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: voice-bank-bot
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: voice-bank-bot
  minReplicas: 1
  maxReplicas: 5
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 50
